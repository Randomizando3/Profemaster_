<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ProfeMaster.Pages.Help.ContactPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="#FFFFFF"
    Title=""
    Shell.NavBarIsVisible="False">

    <ContentPage.Resources>
        <Color x:Key="PmText">#2B2B2B</Color>
        <Color x:Key="PmMuted">#7B7B7B</Color>
        <Color x:Key="PmStroke">#E6E6E6</Color>
        <Color x:Key="PmSoft">#F2F2F2</Color>

        <LinearGradientBrush x:Key="PmGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#B66AF4" Offset="0" />
            <GradientStop Color="#7E97D9" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="PmCard" TargetType="Border">
            <Setter Property="BackgroundColor" Value="#FFFFFF" />
            <Setter Property="Stroke" Value="{StaticResource PmStroke}" />
            <Setter Property="StrokeThickness" Value="2" />
            <Setter Property="StrokeShape" Value="RoundRectangle 18" />
            <Setter Property="Padding" Value="14" />
        </Style>

        <Style x:Key="PmField" TargetType="Border">
            <Setter Property="BackgroundColor" Value="#FFFFFF" />
            <Setter Property="Stroke" Value="{StaticResource PmStroke}" />
            <Setter Property="StrokeThickness" Value="2" />
            <Setter Property="StrokeShape" Value="RoundRectangle 16" />
            <Setter Property="Padding" Value="12,10" />
        </Style>

        <Style x:Key="PmBtnPrimary" TargetType="Button">
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="TextColor" Value="#FFFFFF" />
            <Setter Property="Background" Value="{StaticResource PmGradient}" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="HeightRequest" Value="52" />
        </Style>

        <Style x:Key="PmBtnSoft" TargetType="Button">
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="TextColor" Value="{StaticResource PmText}" />
            <Setter Property="BackgroundColor" Value="{StaticResource PmSoft}" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="HeightRequest" Value="52" />
        </Style>
    </ContentPage.Resources>

    <Grid Padding="18" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
            <Border BackgroundColor="#FFFFFF"
                    Stroke="{StaticResource PmStroke}"
                    StrokeThickness="2"
                    StrokeShape="RoundRectangle 16"
                    Padding="10,8"
                    VerticalOptions="Center">
                <Button Text="Voltar"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource PmText}"
                        FontAttributes="Bold"
                        FontSize="12"
                        Padding="4,0"
                        Clicked="OnBack"/>
            </Border>

            <VerticalStackLayout Grid.Column="1" Spacing="0" VerticalOptions="Center">
                <Label Text="Contato"
                       FontSize="16"
                       FontAttributes="Bold"
                       TextColor="{StaticResource PmText}" />
                <Label Text="Sugestões e dúvidas viram ticket."
                       FontSize="11"
                       TextColor="{StaticResource PmMuted}" />
            </VerticalStackLayout>

            <Border Grid.Column="2"
                    BackgroundColor="{StaticResource PmSoft}"
                    Stroke="{StaticResource PmStroke}"
                    StrokeThickness="2"
                    StrokeShape="RoundRectangle 16"
                    Padding="10,8"
                    VerticalOptions="Center">
                <Label Text="Ticket"
                       FontSize="11"
                       TextColor="{StaticResource PmMuted}"
                       VerticalOptions="Center"/>
            </Border>
        </Grid>

        <!-- CONTENT -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="12">

                <Border Style="{StaticResource PmCard}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Envie uma mensagem"
                               FontAttributes="Bold"
                               FontSize="14"
                               TextColor="{StaticResource PmText}"/>

                        <Label Text="Preencha seu nome e descreva sua sugestão/dúvida."
                               FontSize="10"
                               TextColor="{StaticResource PmMuted}"/>

                        <Label Text="Seu nome" FontSize="10" TextColor="{StaticResource PmMuted}" />
                        <Border Style="{StaticResource PmField}">
                            <Entry x:Name="NameEntry"
                                   Placeholder="Ex: Maria"
                                   BackgroundColor="Transparent"
                                   TextColor="{StaticResource PmText}"
                                   PlaceholderColor="{StaticResource PmMuted}"
                                   FontSize="13"/>
                        </Border>

                        <Label Text="Mensagem" FontSize="10" TextColor="{StaticResource PmMuted}" />
                        <Border Style="{StaticResource PmField}">
                            <Editor x:Name="MessageEditor"
                                    AutoSize="TextChanges"
                                    Placeholder="Escreva aqui..."
                                    BackgroundColor="Transparent"
                                    TextColor="{StaticResource PmText}"
                                    PlaceholderColor="{StaticResource PmMuted}"
                                    FontSize="13"
                                    HeightRequest="140"/>
                        </Border>

                        <Label x:Name="StatusLabel"
                               Text=""
                               FontSize="11"
                               TextColor="{StaticResource PmMuted}"/>
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>

        <!-- BOTTOM -->
        <Grid Grid.Row="2" ColumnDefinitions="*,*" ColumnSpacing="10">
            <Button Text="Limpar"
                    Style="{StaticResource PmBtnSoft}"
                    Clicked="OnClear"/>

            <Button Grid.Column="1"
                    Text="Enviar"
                    Style="{StaticResource PmBtnPrimary}"
                    Clicked="OnSend"/>
        </Grid>

    </Grid>
</ContentPage>
